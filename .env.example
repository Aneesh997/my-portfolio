# Copy this file to .env.local (or .env) and adjust as needed.
# URL of the deployed backend (aneesh-portfolio/server) — used by the
# contact form. Point this at your Render URL after deploying the backend.
VITE_API_BASE_URL=http://localhost:5001
